name: Lab 4 Workflow

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello from GitHub Actions"

      - name: Show that a secret exists
        run: echo "Secret is configured"
        env:
          EXERCISE_PAT: ${{ secrets.EXERCISE_PAT }}